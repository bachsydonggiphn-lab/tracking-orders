# Cổng chạy ứng dụng (Mặc định 5000 cho local, Render sẽ tự cấp PORT trên Cloud)
PORT=5000

# Cấu hình Database Turso (Cloud LibSQL)
# Bỏ trống nếu muốn chạy offline với file SQLite local (data/tracking.sqlite)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Khóa API dự phòng (nếu có)
GEMINI_API_KEY=
